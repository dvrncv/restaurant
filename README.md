# Restaurant SOP - Инструкция для зачета

## Порты сервисов

| 8080 | Demo REST API | Основной REST API |
| 8081 | Analytics Service | HTTP/Actuator метрики |
| 8082 | Audit Service | Сервис аудита |
| 8083 | Notification Service | Сервис уведомлений |
| 8085 | Jenkins | CI/CD сервер |
| 9090 | Analytics Service | gRPC порт |
| 9091 | Prometheus | Метрики |
| 9411 | Zipkin | Трассировка |
| 3000 | Grafana | Визуализация метрик |
| 5672 | RabbitMQ | Брокер сообщений |
| 15672 | RabbitMQ Management | UI управления |


## Шаг 1: Подготовка проекта

### Вариант A: С GitHub

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd restaurant
```

### Вариант B: С флешки

```bash
# Скопируйте папку проекта на компьютер
# Откройте терминал в папке проекта
cd restaurant
```

---

## Шаг 2: Проверка зависимостей

Убедитесь, что установлены:
- Docker
- Docker Compose

Проверка:
```bash
docker --version
docker-compose --version
```

---

## Шаг 3: Подготовка контрактов (если нужно)

Если контракты еще не собраны, выполните:

```bash
# Windows PowerShell
cd events-contract-restaurant
.\mvnw.cmd install
cd ..

cd api-contract-restaurant
.\mvnw.cmd install
cd ..

# Linux/Mac
cd events-contract-restaurant
./mvnw install
cd ..

cd api-contract-restaurant
./mvnw install
cd ..
```

**Примечание:** Если контракты уже собраны и находятся в локальном Maven репозитории (~/.m2/repository), этот шаг можно пропустить.

---

## Шаг 4: Сборка проектов (ОБЯЗАТЕЛЬНО!)

**ВАЖНО:** Перед запуском Docker Compose необходимо собрать все проекты, чтобы создать JAR файлы.

### 4.1. Сборка контрактов

```bash
# Windows PowerShell
cd events-contract-restaurant
.\mvnw.cmd install
cd ..

cd api-contract-restaurant
.\mvnw.cmd install
cd ..

# Linux/Mac
cd events-contract-restaurant
./mvnw install
cd ..

cd api-contract-restaurant
./mvnw install
cd ..
```

### 4.2. Сборка всех сервисов

```bash
# Windows PowerShell - из корня проекта
.\mvnw.cmd clean package -DskipTests

# Linux/Mac - из корня проекта
./mvnw clean package -DskipTests
```

Или по отдельности:

```bash
# Windows PowerShell
cd analytics-service-restaurant
.\mvnw.cmd clean package -DskipTests
cd ..

cd audit-service-restaurant
.\mvnw.cmd clean package -DskipTests
cd ..

cd notification-service-restaurant
.\mvnw.cmd clean package -DskipTests
cd ..

cd rest-restaurant
.\mvnw.cmd clean package -DskipTests
cd ..
```

**Проверка:** Убедитесь, что в каждой папке `target/` есть JAR файл:
- `analytics-service-restaurant/target/*.jar`
- `audit-service-restaurant/target/*.jar`
- `notification-service-restaurant/target/*.jar`
- `rest-restaurant/target/*.jar`

---

## Шаг 5: Запуск инфраструктуры и сервисов

### 5.1. Запуск всех сервисов через Docker Compose

```bash
# Из корня проекта
docker-compose up -d
```

Эта команда запустит:
- RabbitMQ (5672, 15672)
- Zipkin (9411)
- Prometheus (9091)
- Grafana (3000)
- Analytics Service (8081, 9090)
- Audit Service (8082)
- Notification Service (8083)
- Demo REST API (8080)

### 4.2. Проверка статуса контейнеров

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` или `Up (healthy)`.

### 4.3. Просмотр логов (опционально)

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f demo-rest
docker-compose logs -f analytics-service
```

---

## Шаг 6: Запуск Jenkins

### 6.1. Запуск Jenkins

```bash
cd jenkins
docker-compose up -d
cd ..
```

### 6.2. Проверка запуска Jenkins

```bash
docker ps | grep jenkins
```

Jenkins должен быть доступен по адресу: **http://localhost:8085**

### 6.3. Получение начального пароля

```bash
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

Скопируйте пароль - он понадобится для первого входа.

### 6.4. Первый вход в Jenkins

1. Откройте браузер: http://localhost:8085
2. Вставьте скопированный пароль
3. Нажмите "Continue"
4. Выберите "Install suggested plugins"
5. Дождитесь установки плагинов
6. Создайте администратора (или пропустите, если используете существующего)

---

## Шаг 7: Настройка Jenkins для сборки проекта

### 7.1. Установка необходимых плагинов

1. В Jenkins перейдите: **Manage Jenkins** → **Plugins**
2. Установите следующие плагины (если не установлены):
   - **Maven Integration**
   - **Pipeline**
   - **Docker Pipeline**
   - **Git**

### 7.2. Настройка инструментов (ОБЯЗАТЕЛЬНО!)

Jenkinsfile использует Maven и JDK из Jenkins Tools, поэтому их нужно настроить:

1. **Manage Jenkins** → **Tools** (или **Global Tool Configuration**)
2. **JDK installations**:
   - Нажмите **Add JDK**
   - **Name**: `JDK 17` (важно: именно это имя!)
   - **JAVA_HOME**: `/usr/lib/jvm/java-17-openjdk-amd64` (или путь к JDK в контейнере)
   - Или выберите **Install automatically** → выберите версию **17** (имя будет автоматически "JDK 17")
3. **Maven installations**:
   - Нажмите **Add Maven**
   - **Name**: `Maven` (важно: именно это имя!)
   - **MAVEN_HOME**: `/usr/share/maven` (или путь к Maven в контейнере)
   - Или выберите **Install automatically** → выберите версию (например, **3.9.5**)
4. Нажмите **Save**

**Важно**: Имена инструментов должны точно совпадать с именами в Jenkinsfile:
- JDK: `JDK 17`
- Maven: `Maven`

### 7.3. Настройка Docker

Docker CLI автоматически устанавливается в Jenkins контейнер через `jenkins/Dockerfile`.

Если Docker все еще не работает после перезапуска контейнера, проверьте:
```bash
docker exec jenkins docker --version
```

Если команда не работает, пересоздайте Jenkins контейнер:
```bash
cd jenkins
docker-compose down
docker-compose build
docker-compose up -d
```

---

## Шаг 8: Создание и запуск Pipeline Job

### 8.1. Создание Pipeline Job

1. В Jenkins нажмите **New Item**
2. Введите имя: `restaurant-sop-build`
3. Выберите **Pipeline**
4. Нажмите **OK**

### 8.2. Настройка Pipeline

**Вариант A: Pipeline script directly (рекомендуется для зачета)**

**ВАЖНО:** Для этого варианта нужно пробросить папку проекта в Jenkins контейнер.

1. Остановите Jenkins:
   ```bash
   cd jenkins
   docker-compose down
   ```

2. Отредактируйте `jenkins/docker-compose.yml` - volume уже добавлен автоматически
   (Если нужно добавить вручную, добавьте строку: `- ../:/workspace:ro` в секцию volumes)

3. Запустите Jenkins снова:
   ```bash
   docker-compose up -d
   cd ..
   ```

4. В Jenkins:
   - Прокрутите вниз до раздела **Pipeline**
   - В **Definition** выберите: **Pipeline script**
   - Откройте файл `Jenkinsfile.direct` из корня проекта в текстовом редакторе
   - Скопируйте весь его содержимое (Ctrl+A, Ctrl+C)
   - Вставьте содержимое в поле **Script** в Jenkins
   - Нажмите **Save**

**Альтернатива (без изменения docker-compose):**

Если не хотите менять docker-compose, можно использовать **Copy Artifact Plugin** или просто скопировать проект в workspace Jenkins вручную через команды в Pipeline.

**Вариант B: Pipeline script from SCM (если проект на GitHub)**

1. Прокрутите вниз до раздела **Pipeline**
2. В **Definition** выберите: **Pipeline script from SCM**
3. **SCM**: выберите **Git**
4. **Repository URL**: вставьте URL репозитория
5. **Credentials**: 
   - Нажмите **Add** → **Jenkins**
   - **Kind**: Username with password
   - **Username**: ваш GitHub username
   - **Password**: Personal Access Token (не пароль!)
   - Как создать токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token (нужны права `repo`)
6. **Branch Specifier**: `*/main` или `*/master`
7. **Script Path**: `Jenkinsfile`
8. Нажмите **Save**

**Вариант C: Локальный путь (если проект на компьютере)**

1. Пробросьте папку проекта в Jenkins контейнер:
   - Отредактируйте `jenkins/docker-compose.yml`
   - Добавьте volume: `- /путь/к/проекту:/workspace:ro`
2. В Pipeline используйте: **Pipeline script from SCM** → **Git** → `file:///workspace`

### 8.3. Запуск сборки

1. На странице проекта нажмите **Build Now**
2. Сборка начнется автоматически

### 8.4. Мониторинг сборки

1. Нажмите на номер сборки (например, #1)
2. Нажмите **Console Output** для просмотра логов
3. Дождитесь завершения сборки

**Ожидаемые этапы сборки:**
- ✅ Checkout
- ✅ Make Maven Wrapper Executable
- ✅ Build Parent POM
- ✅ Build Contracts (параллельно)
- ✅ Build Services (параллельно)
- ✅ Build Docker Images (параллельно)

**Время сборки:** примерно 5-10 минут (первый раз может быть дольше из-за загрузки зависимостей)

### 8.5. Проверка успешности сборки

Сборка считается успешной, если:
- В консоли видно: `Build completed successfully!`
- Статус сборки: **Success** (синий кружок)
- Все этапы отмечены зелеными галочками

---

## Шаг 9: Показ метрик

### 9.1. Prometheus метрики

1. Откройте браузер: **http://localhost:9091**
2. В поисковой строке введите метрику, например:
   - `jvm_memory_used_bytes` - использование памяти JVM
   - `http_server_requests_seconds_count` - количество HTTP запросов
   - `process_cpu_usage` - использование CPU
3. Нажмите **Execute** для просмотра графика
4. Перейдите на вкладку **Graph** для визуализации

**Примеры запросов:**
```
# Общее использование памяти всеми сервисами
sum(jvm_memory_used_bytes)

# Количество HTTP запросов к REST API
http_server_requests_seconds_count{application="rest-restaurant"}

# Использование CPU
process_cpu_usage{application="analytics-service-restaurant"}
```

### 9.2. Grafana дашборды

1. Откройте браузер: **http://localhost:3000**
2. Войдите:
   - **Username**: `admin`
   - **Password**: `admin`
3. При первом входе измените пароль (или пропустите)

#### Настройка источника данных Prometheus:

1. Нажмите **Configuration** (шестеренка) → **Data Sources**
2. Нажмите **Add data source**
3. Выберите **Prometheus**
4. В **URL** введите: `http://prometheus:9090`
5. Нажмите **Save & Test**
6. Должно появиться: **Data source is working**

#### Создание простого дашборда:

1. Нажмите **+** → **Create Dashboard**
2. Нажмите **Add visualization**
3. Выберите источник данных: **Prometheus**
4. В поле запроса введите: `jvm_memory_used_bytes`
5. Нажмите **Run query**
6. Нажмите **Apply** для сохранения панели

**Готовые дашборды:**
- Можно импортировать готовые дашборды для Spring Boot из Grafana Dashboard Library
- ID популярных дашбордов: 11378, 4701

### 9.3. Actuator метрики (прямой доступ)

Метрики доступны напрямую через Actuator endpoints:

- **Demo REST API**: http://localhost:8080/actuator/prometheus
- **Analytics Service**: http://localhost:8081/actuator/prometheus
- **Audit Service**: http://localhost:8082/actuator/prometheus
- **Notification Service**: http://localhost:8083/actuator/prometheus

**Health checks:**
- http://localhost:8080/actuator/health
- http://localhost:8081/actuator/health
- http://localhost:8082/actuator/health
- http://localhost:8083/actuator/health

### 9.4. RabbitMQ метрики

1. Откройте браузер: **http://localhost:15672**
2. Войдите:
   - **Username**: `guest`
   - **Password**: `guest`
3. Перейдите в раздел **Queues** для просмотра очередей
4. Раздел **Overview** показывает общую статистику

---

## Шаг 10: Проверка работы приложения

### 10.1. Проверка доступности сервисов

```bash
# Проверка через curl (или используйте браузер)
curl http://localhost:8080/actuator/health
curl http://localhost:8081/actuator/health
curl http://localhost:8082/actuator/health
curl http://localhost:8083/actuator/health
```

Все должны вернуть: `{"status":"UP"}`

### 10.2. Тестовый запрос к REST API

```bash
# Получить список блюд (если такой endpoint есть)
curl http://localhost:8080/api/dishes

# Или через браузер
# http://localhost:8080/api/dishes
```

---

## Шаг 11: Трассировка (Zipkin)

1. Откройте браузер: **http://localhost:9411**
2. Нажмите **Run Query** для просмотра трассировок
3. После выполнения запросов к приложению, трассировки появятся в Zipkin

---

## Возможные проблемы и решения

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверьте логи
docker-compose logs

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d
```

### Проблема: Порт уже занят

**Решение:**
```bash
# Найдите процесс, использующий порт (Windows)
netstat -ano | findstr :8080

# Остановите контейнеры
docker-compose down

# Или измените порт в docker-compose.yml
```

### Проблема: Jenkins не может собрать проект

**Решение:**
1. Проверьте, что Jenkinsfile находится в корне проекта
2. Проверьте логи сборки в Jenkins Console Output
3. Убедитесь, что Maven Wrapper доступен (файлы mvnw, mvnw.cmd)
4. Проверьте права доступа к файлам

### Проблема: Метрики не отображаются в Prometheus

**Решение:**
1. Проверьте, что все сервисы запущены: `docker-compose ps`
2. Проверьте конфигурацию Prometheus: `cat prometheus.yml`
3. Проверьте доступность метрик: `curl http://localhost:8080/actuator/prometheus`
4. Перезапустите Prometheus: `docker-compose restart prometheus`

### Проблема: Grafana не подключается к Prometheus

**Решение:**
1. В Grafana используйте URL: `http://prometheus:9090` (не localhost!)
2. Проверьте, что Prometheus и Grafana в одной сети Docker
3. Проверьте логи Grafana: `docker-compose logs grafana`

---

## Быстрая справка команд

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f [service_name]

# Перезапуск сервиса
docker-compose restart [service_name]

# Запуск Jenkins
cd jenkins && docker-compose up -d && cd ..

# Остановка Jenkins
cd jenkins && docker-compose down && cd ..

# Проверка портов
netstat -ano | findstr :8080  # Windows
lsof -i :8080                 # Linux/Mac
```

---

## Чек-лист для зачета

- [ ] Все контейнеры запущены и работают (`docker-compose ps`)
- [ ] Jenkins запущен и доступен (http://localhost:8085)
- [ ] Pipeline Job создан и настроен
- [ ] Сборка выполнена успешно (статус Success)
- [ ] Prometheus доступен и собирает метрики (http://localhost:9091)
- [ ] Grafana подключена к Prometheus
- [ ] Метрики отображаются в Grafana
- [ ] Все сервисы отвечают на health checks
- [ ] RabbitMQ Management доступен (http://localhost:15672)
- [ ] Zipkin доступен (http://localhost:9411)

---

## Дополнительная информация

### Структура сборки в Jenkins

Jenkinsfile выполняет следующие этапы:
1. **Checkout** - получение кода из репозитория
2. **Build Parent POM** - установка родительского POM
3. **Build Contracts** - сборка контрактов (параллельно)
4. **Build Services** - сборка всех сервисов (параллельно)
5. **Build Docker Images** - создание Docker образов (параллельно)

### Архитектура системы

```
┌─────────────┐
│  REST API   │───gRPC───► Analytics Service
│  (8080)     │
└──────┬──────┘
       │
       │ RabbitMQ
       │
   ┌───┴───┬──────────┬─────────────┐
   │       │          │             │
Audit   Notification  Prometheus   Zipkin
(8082)    (8083)      (9091)       (9411)
```

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Убедитесь, что все порты свободны
4. Проверьте, что Docker и Docker Compose установлены и работают

---

**Удачи на зачете! 🚀**

